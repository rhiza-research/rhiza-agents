# Default values for rhiza-agents.

# Container image configuration
image:
  repository: ghcr.io/rhiza-research/rhiza-agents
  tag: latest
  pullPolicy: Always

# Image pull secrets (not needed for public GHCR repos)
imagePullSecrets: []

# Service configuration
service:
  type: ClusterIP
  port: 8123

# Resource configuration
resources:
  limits:
    cpu: 1000m
    memory: 2Gi
  requests:
    cpu: 250m
    memory: 512Mi

# Ingress configuration
ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts: []
  tls: []

# Persistent volume for LangGraph Server data
persistence:
  size: 5Gi
  storageClassName: standard-rwo

# Name of the Kubernetes secret containing sensitive env vars.
# This secret is created by Terraform, NOT by this chart.
# Keys: ANTHROPIC_API_KEY, DAYTONA_API_KEY, LANGCHAIN_API_KEY
secretName: rhiza-agents-secrets

# Non-secret environment variables
env:
  MCP_SERVER_URL: ""
  LANGCHAIN_TRACING_V2: "true"
  LANGCHAIN_PROJECT: "rhiza-agents"
