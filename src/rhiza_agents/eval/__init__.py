"""Offline evaluation runner for the rhiza-agents supervisor graph.

Runs Langfuse datasets against the same graph the chat app uses, with each
item invocation linked to a Langfuse dataset run for side-by-side comparison
across experiments.
"""
